{
  "name": "replicate-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that holds the Replicate API token so the Enhanzo app never has to.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.42.0"
  }
}
