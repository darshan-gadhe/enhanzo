<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Uploading a photo to Replicate and downloading the enhanced result.
         The Flutter tool adds this to debug/profile builds automatically; a
         release build has to declare it here or every request fails. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Google Play Billing: required for the subscription plans on the
         paywall to launch a real purchase flow instead of the simulated one. -->
    <uses-permission android:name="com.android.vending.BILLING"/>
    <application
        android:label="Enhanzo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Fall back to the Skia renderer instead of Impeller.

             Not a precaution: Impeller's OpenGL ES backend aborts this app on
             launch, and the abort names its own source line —

               [FATAL:flutter/impeller/renderer/backend/gles/blit_pass_gles.cc(88)]
               Check failed: result. Must be able to encode GL commands
               without error.

             — signal 6, before the first frame. Captured on an Android 16
             emulator, and the same signature devices with weaker GLES drivers
             report; a Xiaomi launch crash that could not be reproduced here
             for weeks is very likely this.

             Impeller is the better renderer where it works. It cannot be the
             renderer on hardware where it aborts, and there is no way for the
             app to detect that before it has already crashed. Revisit when
             Flutter's GLES backend stops failing this check. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
